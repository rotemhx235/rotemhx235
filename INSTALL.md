# איך מתקינים את ״גדול״ על הטלפון

מדריך מלא, צעד אחר צעד. אין צורך במחשב ואין צורך בידע טכני.
כל התהליך לוקח כ-5 דקות, ורק בפעם הראשונה.

---

## לפני שמתחילים — רק לסמסונג

**אם הטלפון שלך הוא סמסונג יחסית חדש, צריך לעשות שלב אחד לפני הכל.**

סמסונג הוסיפה הגנה בשם **Auto Blocker** שחוסמת התקנה של אפליקציות
שלא הגיעו מחנות רשמית. היא **מופעלת מראש** בדגמים חדשים, ובלי לכבות
אותה ההתקנה פשוט תיחסם עם הודעה בסגנון ״אפליקציה לא מוכרת נחסמה״.

איך לכבות:

1. לפתוח **הגדרות** (Settings).
2. לגלול ל-**אבטחה ופרטיות** (Security and privacy).
3. לבחור **Auto Blocker**.
4. לכבות את המתג.

> אם לא מצאת את האפשרות — כנראה שהטלפון שלך ישן יותר ואין לו את ההגנה
> הזאת בכלל. זה בסדר גמור, אפשר להמשיך הלאה.

לאחר שתסיים להתקין, אפשר להדליק את Auto Blocker בחזרה אם תרצה.

---

## שלב 1 — להוריד את האפליקציה

**הדרך הכי פשוטה: ישירות מהטלפון.**

1. לפתוח את הדפדפן **בטלפון** (Chrome / Samsung Internet).
2. להיכנס לכתובת:
   **https://github.com/rotemhx235/rotemhx235/releases/latest**
3. לגלול למטה עד לקטע **Assets**.
4. ללחוץ על הקובץ **`gadol.apk`**. ההורדה מתחילה.
5. הדפדפן עשוי להזהיר ״קובץ מסוג זה עלול להזיק למכשיר״ —
   ללחוץ **הורדה בכל זאת** (Download anyway).

> זו אזהרה שגרתית שאנדרואיד מציג לכל קובץ APK, בלי קשר לתוכן שלו.

---

## שלב 2 — להתקין

1. לפתוח את **ההורדות** בטלפון (או ללחוץ על ההתראה של ההורדה שהסתיימה).
2. ללחוץ על **`gadol.apk`**.
3. אנדרואיד יגיד שאין הרשאה להתקין מהמקור הזה, ויציע כפתור **הגדרות**.
   ללחוץ עליו, ולהפעיל את **״אפשר מהמקור הזה״**.
4. לחזור אחורה. ללחוץ **התקנה** (Install).
5. אם מופיעה הודעה של Play Protect בסגנון ״שליחה לבדיקה״ —
   ללחוץ **התקנה בכל זאת** (Install anyway).

סיימת. אייקון בשם **גדול** יופיע במסך האפליקציות.

---

## שלב 3 — הפעלה ראשונה

1. לפתוח את **גדול**.
2. ללחוץ על הכפתור הכחול הגדול — **״הפעלת ההגדלה״**.
   זה יפתח את מסך הנגישות של הטלפון.
3. למצוא את **גדול** ברשימה וללחוץ עליו.
   - **בסמסונג** הנתיב הוא: הגדרות ← נגישות ← **אפליקציות מותקנות**
     (Installed apps) ← גדול.
   - באנדרואיד רגיל: הגדרות ← נגישות ← **שירותים מותקנים** ← גדול.
4. להפעיל את המתג ולאשר את חלון האישור שקופץ.
5. ללחוץ על כפתור החזרה עד שחוזרים לאפליקציה.

**עכשיו אמור להופיע כפתור עגול כחול על המסך.** הוא יישאר שם תמיד,
מעל כל האפליקציות.

---

## איך משתמשים

- **לחיצה על הכפתור העגול** — מגדילה את המסך. הכפתור הופך לאדום עם X.
- **לחיצה נוספת** — חוזרים לגודל רגיל.
- **גרירה של הכפתור** — מזיזה אותו לכל מקום במסך.
  כשההגדלה פעילה, הגרירה גם מזיזה את מה שרואים מוגדל.
- **לשינוי עוצמת ההגדלה** — לפתוח את האפליקציה ולשנות בקטע ״כמה להגדיל״.

## שינוי גודל הטקסט בכל הטלפון

זו יכולת נפרדת בתוך האפליקציה, והיא דורשת אישור נוסף:

1. באפליקציה, ללחוץ על **+** או **−** בקטע ״גודל הטקסט בטלפון״.
2. ייפתח מסך הגדרות. להפעיל את **״אפשר שינוי הגדרות מערכת״**.
3. לחזור לאפליקציה. עכשיו הכפתורים עובדים.

> בחלק ממכשירי סמסונג, סמסונג חוסמת שינוי של הגדרה זו מתוך אפליקציות.
> במקרה כזה האפליקציה תודיע על כך ותפתח עבורך את מסך ההגדרות המתאים
> ישירות — עדיין חוסך את החיפוש בתפריטים.

---

## אם משהו לא עובד

| מה קרה | מה לעשות |
|---|---|
| ההתקנה נחסמה | לכבות את Auto Blocker (ראה למעלה) |
| הכפתור העגול לא מופיע | לוודא שהשירות באמת הופעל בהגדרות הנגישות |
| הכפתור מופיע אבל ההגדלה לא עובדת | לכבות ולהפעיל מחדש את השירות |
| הכפתור נעלם אחרי כמה שעות | הגדרות ← בטרייה ← למצוא את גדול ולסמן ״ללא הגבלה״ |

בכל מקרה אחר — לצלם מסך ולשלוח, נתקן.

---

## למה זה לא מהחנות של גוגל?

האפליקציה עדיין בפיתוח. פרסום ב-Google Play דורש חשבון מפתח בתשלום
(25$ חד-פעמי) ותהליך אישור. נגיע לזה כשהאפליקציה תהיה בשלה.
בינתיים זו הדרך לבדוק אותה על מכשירים אמיתיים.
